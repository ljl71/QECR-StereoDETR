# 第二创新点 V12：分组相关信息保留与轻量门控路线论证

> 2026-08-11更新：V12-G0已在100个样本上正式通过。最佳组合为s4、16组；
> 目标和Car的Oracle ±1 bin命中率分别提升30.03和30.65个百分点，MAE相对下降
> 73.74%和73.01%。由于固定分组最大聚合反而劣于原均值，正式实现采用零初始化
> 空间门控，而不是max。当前只授权V12O/V12A五轮配对，不授权195轮完整训练。

## 结论先行

V12 当前只实现 **G0 证据诊断**，尚未修改 StereoDETR 主模型。原因不是路线不明确，而是必须先证明当前相关体在全通道平均时确实丢失了与 KITTI 目标有关的匹配信息。只有正式 G0 通过预注册门槛，才实现 V12A 的轻量门控并做 5 轮同起点配对；若 G0 否决，就停止该路线，不消耗完整训练费用。

当前最有逻辑、同时最符合低时延约束的候选不是完整替换成立体匹配网络，而是：

> 保留多个通道组的相关性提议，用一个零初始化、图像位置相关但与视差共享的微型门控将其压回原有 D 通道，再无缝送入当前 LightStereo 2D 聚合器。

这条路线针对的是 StereoDETR 当前代码中一个可以精确定位的缺陷：`correlation_volume` 在构建每个视差平面时立即对 256 个特征通道做 `mean(dim=1)`。不同通道子空间的匹配证据在进入聚合网络前已经被压成一个标量，后面的注意力只能重标定已经压缩后的 D 个视差通道，无法恢复被平均掉的信息。

## 一、当前基线究竟有什么、缺什么

当前深度预测器在 s4、s8、s16 三层分别建立相关体：

- s4：24 个视差通道，对应输入图像最大约 96 px；
- s8：24 个视差通道，对应输入图像最大约 192 px；
- s16：12 个视差通道，对应输入图像最大约 192 px。

每个位置、每个视差只保留一个全通道平均相关值：

\[
c(d,p)=\frac{1}{C}\sum_{k=1}^{C}f^L_k(p)f^R_k(p-d).
\]

代码随后使用与 LightStereo 一致的轻量 2D 聚合结构，并在三个尺度上用左图特征生成通道注意力。因此必须实事求是地区分：

- **已经存在**：LightStereo 的 2D 代价聚合、左图引导的通道增强、多尺度代价融合；
- **不存在**：GwcNet 式多个通道子空间的匹配提议；
- **不能重复包装**：CoEx 的图像引导代价激励、LightStereo 的 channel boost；
- **真正可补的缺口**：在第一次全通道平均之前保留匹配子空间差异。

## 二、检索的论文与官方代码

### 1. GwcNet：核心理论来源，适配度最高

GwcNet 指出 full correlation 会丢失过多信息，并把左右特征按通道划分为若干组，每组独立相关，从而形成多个匹配代价提议。其论文实验还显示，在降低后续网络参数时，分组相关比单一相关更能保持精度。[CVPR 2019 论文与摘要](https://openaccess.thecvf.com/content_CVPR_2019/html/Guo_Group-Wise_Correlation_Stereo_Network_CVPR_2019_paper.html)，[官方代码](https://github.com/xy-guo/GwcNet)

本地官方代码固定版本：

- 目录：`code/stereo_matching_modules/GwcNet`
- commit：`0987074f48b098c6e975a58ca5aacf233b0fb0eb`
- 可复用逻辑：`models/submodule.py` 中的 `groupwise_correlation`；
- 不直接复用部分：其完整 G×D 体和 3D hourglass，会明显增加显存与时延。

### 2. LightStereo：确认现有基线能力，不能当新模块

LightStereo 把视差维作为 2D 代价体的通道，并使用图像特征增强关键代价通道；论文报告了 22 GFLOPs 和 17 ms 的轻量结果。[LightStereo 论文](https://arxiv.org/abs/2406.19833)，[OpenStereo 官方代码](https://github.com/XiandaGuo/OpenStereo)

本地官方代码固定版本：

- 目录：`code/stereo_matching_modules/OpenStereo`
- commit：`23d71c92e33ad1f80dfc42bf29f5c6a914d38769`

当前 StereoDETR 的 `Aggregation` 与 `AttentionModule` 已经属于这一技术路线，所以 V12 不再添加同类 channel boost。

### 3. CoEx：图像引导思想有用，但模块已经被基线覆盖

CoEx 用左图空间特征为代价体生成通道权重，强调以很小额外开销提高匹配质量。[CoEx 论文](https://arxiv.org/abs/2108.05773)，[官方代码](https://github.com/antabangun/coex)

本地官方代码固定版本：

- 目录：`code/stereo_matching_modules/CoEx-official`
- commit：`a98b37d33f37754582b1e71f38216af8c69f2347`

它对 V12 的启发仅是“门控可以由左图位置引导并在视差维共享”；不能把 GCE 原样搬入，因为现有 `att0/att2/att4` 已经完成同类作用。

### 4. BANet：痛点分析高度相关，但原版双聚合分支偏重

BANet 分析了纯 2D 代价聚合容易模糊边界、损失细节并在弱纹理区域误匹配的问题。它用空间注意图将代价分成 detail/smooth 两部分，再用两个独立聚合器处理。论文的 2D 消融中 MACs 从单分支 29G 增到 39G，EPE 从 0.63 降到 0.57；说明方向有效，但计算增幅不适合本项目直接照搬。[ICCV 2025 论文](https://openaccess.thecvf.com/content/ICCV2025/html/Xu_BANet_Bilateral_Aggregation_Network_for_Mobile_Stereo_Matching_ICCV_2025_paper.html)，[官方代码](https://github.com/gangweix/BANet)

本地官方代码固定版本：

- 目录：`code/stereo_matching_modules/BANet`
- commit：`7cb83f7cca99a249e1fcec8ff556e4ac73facd70`
- 审计结论：`banet-2d/core/BANet.py` 确实实例化了 `cost_agg0` 和 `cost_agg1` 两套完整聚合器；暂不搬入。

### 5. CREStereo：支持“分组相关需要适应位置”，但递归主干不能搬

CREStereo 提出 Adaptive Group Correlation Layer，针对细结构和非理想极线校正；官方实现把 256 通道分成 4 组，并为搜索位置预测偏移，但依赖多尺度递归更新与局部 Transformer。[CVPR 2022 Oral 论文](https://openaccess.thecvf.com/content/CVPR2022/html/Li_Practical_Stereo_Matching_via_Cascaded_Recurrent_Network_With_Adaptive_Correlation_CVPR_2022_paper.html)，[官方代码](https://github.com/megvii-research/CREStereo)

本地官方代码固定版本：

- 目录：`code/stereo_matching_modules/CREStereo`
- commit：`ad3a1613bdedd88b93247e5f002cb7c80799762d`
- 可借鉴：通道组不应被过早平均、组的有效性随图像位置变化；
- 不搬入：递归更新、LoFTR、自适应二维偏移搜索，时延和改动范围都过大。

### 6. ACVNet：证明确保相关线索有价值，但接口过重

ACVNet 用相关线索生成注意权重来过滤拼接特征体，并通过多层自适应 patch matching 增强弱纹理匹配；论文证明代价体表达本身会显著影响精度。[CVPR 2022 论文](https://openaccess.thecvf.com/content/CVPR2022/html/Xu_Attention_Concatenation_Volume_for_Accurate_and_Efficient_Stereo_Matching_CVPR_2022_paper.html)，[官方代码](https://github.com/gangweiX/ACVNet)

其注意拼接体和 3D 聚合接口会破坏当前 D 通道 2D 聚合器的轻量优势，因此不作为 V12 首选。

## 三、候选方案比较

| 候选 | 针对的痛点 | 预期精度证据 | 对现有代码侵入 | 时延风险 | 决策 |
|---|---|---|---|---|---|
| 再加 CoEx/GCE | 代价通道增强 | 有论文证据 | 小 | 小 | 否决：基线已有 |
| 完整 BANet 双分支 | 边界/弱纹理的 2D 聚合混淆 | ICCV 2025 证据强 | 大 | 中高，两个聚合器 | 暂缓 |
| ACV 拼接体 | 单一相关表达不足 | CVPR 2022 证据强 | 很大 | 高 | 否决 |
| CREStereo AGCL 全套 | 细结构、校正误差 | CVPR 2022 Oral | 很大 | 高 | 否决 |
| G×D 分组体直接入聚合器 | 全平均丢信息 | GwcNet 支持 | 中 | 高，聚合通道增 G 倍 | 否决 |
| **分组相关在线压回 D 通道** | **全平均丢信息** | **GwcNet + G0 实测** | **小** | **低** | **V12 首选** |

## 四、计划中的正式 V12A 结构

若 G0 通过，把 C 个特征通道分为 G 组：

\[
c_g(d,p)=\frac{G}{C}\sum_{k\in g}f^L_k(p)f^R_k(p-d).
\]

门控只由左图同尺度特征生成，并对所有视差共享：

\[
w(p)=\operatorname{softmax}(h(f^L(p))),\quad
c^*(d,p)=\sum_{g=1}^{G}w_g(p)c_g(d,p).
\]

关键安全设计：

1. `h` 的最后一层全零初始化，初始 softmax 为 `1/G`；
2. 此时 `c*` 在数值上等于当前全通道平均相关；
3. 输出仍是 `[B,D,H,W]`，当前 `Aggregation`、checkpoint 其余参数和评估接口不变；
4. 不保存完整 `[B,G,D,H,W]`，而是逐视差计算 `[B,G,H,W]` 后立即加权压回，控制峰值显存；
5. 门控候选优先采用“组内通道均值 → 3×3 depthwise → 1×1 group mixing”，避免昂贵的 `C→G` 大卷积；
6. G0 选出最有证据的单一尺度后，V12A 第一阶段只改该尺度，不同时改三层。

## 五、V12-G0 诊断设计

诊断脚本：`tools/诊断分组相关信息_V12G0.py`

它使用 V09 checkpoint 做正常前向，通过只读 hook 获取送入深度预测器的 s4/s8/s16 左右特征。KITTI Velodyne 点只用于离线构造稀疏真实视差，不进入模型，也不参与检测结果。

扫描：

- 尺度：s4、s8、s16；
- 组数：4、8、16；
- 方法：当前均匀平均、分组最大聚合、逐点最优分组 Oracle；
- 分层：全部、目标框内、目标边界/内部、背景、三段距离、三类目标；
- 指标：预测 bin MAE、±1/±3 bin 命中率、真值窗口排名、Top3 率、匹配间隔。

目标点不仅要落入变换后的 2D 框，还必须与目标中心深度相差不超过 `max(2m, 15%)`，以降低框内背景污染。

### 预注册门槛

正式结论至少需要：

- 100 个随机固定样本；
- 最佳组合不少于 5000 个目标点；
- 不少于 3000 个 Car 点；
- 目标框内 Oracle 的 ±1 bin 命中率提升 ≥ 5 个百分点；
- 目标框内平均 bin 误差相对下降 ≥ 10%；
- Car 的对应两项提升分别 ≥ 3 个百分点和 5%。

只有全部满足，才进入 V12A。Oracle 通过只说明信息存在，正式门控仍必须采用同一 V09 起点、相同种子、相同训练轮数的短程配对；Car 3D AP_R40 Moderate 未提升至少 0.2 AP 时停止，不跑 195 轮。

## 六、当前状态

- 已完成论文和官方代码审计；
- 已下载并固定 GwcNet、LightStereo/OpenStereo、CoEx、BANet、CREStereo 官方代码版本；
- 已实现 V12-G0 诊断、自检、统一输出与 AutoDL 运行脚本；
- **尚未实现或训练正式 V12A**；
- 下一步先执行 20 样本冒烟，确认数据/显存/指标无错误，再执行 100 样本正式诊断。
