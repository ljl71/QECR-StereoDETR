# 下一阶段改进方向全面分析与G5误差归因预注册

## 结论先行

当前不应立即把又一个热门立体模块接进StereoDETR。V11、V12、V13和V14已经分别证明：动态上采样、相关通道门控、Selective-Stereo式频率选择以及固定相关平滑，都可能改善某个代理指标，却无法稳定提高正式KITTI三维检测结果。继续围绕相关体扫结构、核大小或融合比例，成功概率已经明显降低。

下一步改为“先定位三维框误差，再只修最主要组件”。本地已经增加只读诊断`tools/诊断三维框组件误差_G5.py`。它直接读取V09保存的3769个验证集预测，用类别和二维IoU完成一对一匹配，然后分别把预测框的深度、投影中心、完整三维位置、尺寸和朝向替换为真值，重新调用项目内KITTI官方评估代码。该诊断不训练、不加载模型、不修改checkpoint，成本远低于一次五轮配对。

只有G5证明某个组件存在足够大的正式3D AP_R40上限，才实现对应正式版本。当前优先候选是受MonoDGP启发的“立体深度几何残差校正”，但它尚未被宣布为V15，也没有修改正式模型。

## 一、V09当前真正解决了什么

V09由V08O强续训权重和查询级三维质量排序头组成。其主要作用是校准分类置信度与三维定位质量之间的错位：

```text
最终排序分数 = 分类分数 × exp(-深度log方差) × 查询三维质量
```

这一模块已经通过两条配对证据：弱起点上的V06B和强起点上的V09。V09的Car 3D AP_R40 Moderate为`49.8210`，相对V08O的`49.5345`提高约`0.2865 AP`。它是第一创新点的最终组合模型。

但是质量排序只改变候选顺序和置信度，并不修改三维框几何。`lib/helpers/decode_helper.py`仍直接读取`pred_depth`、三维尺寸和投影中心，再通过标定把图像坐标恢复为三维位置。因此，即使某个候选被正确排到前面，它的深度或投影中心仍可能不够精确。

## 二、当前代码中的可验证缺口

### 2.1 最终深度仍是单值读出

官方配置使用`depth_pre_type: one_w_uncertainty`。在`lib/models/monodetr/stereodetr.py`中，立体分支先产生目标级深度图，然后在预测采样点读出`depth_map`；最终输出为：

```python
depth_ave = torch.cat([depth_map, depth_reg[:, :, 1:2]], -1)
```

第一个通道是最终深度，第二个通道是log方差。V09没有开启旧的查询深度分布、几何门控或投影损失。

### 2.2 代码已经计算几何深度，但V09没有使用它

同一个forward中已经根据三维高度、二维框高度和相机焦距计算`depth_geo`。在`one_w_uncertainty`路径中，它只被计算，没有进入最终深度。直接平均或强行门控并不可取：旧V03/V04查询深度融合实验已经低于基线，说明“存在第二个深度”并不等于“融合后会更准”。

### 2.3 V09之后最需要区分的是误差来源，而不是继续猜模块

最终三维IoU受至少四类误差共同影响：

1. 轴向深度误差；
2. 三维中心在图像上的投影误差；
3. 三维尺寸误差；
4. 朝向误差。

过去多个负实验没有先把这四项分开。G5正是为此增加的正式前置门禁。

## 三、2024—2026论文与官方代码复核

| 论文/代码 | 可借鉴机制 | 与当前代码的关系 | 当前决策 |
|---|---|---|---|
| [StereoDETR论文](https://arxiv.org/abs/2511.18788) / [官方代码](https://github.com/shiyi-mu/StereoDETR-OPEN) | 低成本多尺度视差特征、目标级深度图、可微采样 | 当前基线；论文明确把深度作为双目分支与DETR分支的核心连接 | 保留主干，不换骨干、不加完整稠密立体网络 |
| [MonoDGP，CVPR 2025](https://openaccess.thecvf.com/content/CVPR2025/html/Pu_MonoDGP_Monocular_3D_Object_Detection_with_Decoupled-Query_and_Geometry-Error_Priors_CVPR_2025_paper.html) / [官方代码](https://github.com/PuFanqi23/MonoDGP) | 用查询特征预测轻量几何误差，修正透视投影深度 | 与StereoDETR同属MonoDETR代码谱系；本地官方代码只用很小的查询头产生几何深度残差和不确定性 | **G5深度Oracle通过时的第一候选**；只借残差思想，不搬ResNet50、RSH或双解码器 |
| [MonoCD，CVPR 2024](https://openaccess.thecvf.com/content/CVPR2024/html/Yan_MonoCD_Monocular_3D_Object_Detection_with_Complementary_Depths_CVPR_2024_paper.html) / [官方代码](https://github.com/elvintanhust/MonoCD) | 约束多个深度分支的互补性，避免同向高估或低估 | 适合解释“立体深度与几何深度是否同向偏差”；其完整实现基于MonoFlex且环境更老 | 仅在G5证明深度是主瓶颈、后续又证明两种深度误差高度同号时考虑；不直接搬完整分支 |
| [ADStereo，TIP 2025官方代码](https://github.com/cocowy1/ADStereo) | 自适应下采样与视差对齐 | 面向完整稠密立体匹配，通常需要独立立体预训练；当前V11动态上采样和V14平滑已给出负证据 | 暂停，不作为低风险候选 |
| [DDStereo，2026](https://arxiv.org/abs/2606.24805) | 双解码器与紧凑视差表示 | 任务重心是道路异常检测，结构改动大；当前未找到可审计的正式开源实现 | 不作为代码来源，不据此改正式模型 |
| [QAF2D，CVPR 2024](https://openaccess.thecvf.com/content/CVPR2024/html/Ji_Enhancing_3D_Object_Detection_with_2D_Detection-Guided_Query_Anchors_CVPR_2024_paper.html) | 用二维检测生成三维查询锚点 | 当前StereoDETR的二维检测和候选排序已经较强，移植会改查询初始化与训练匹配，风险高 | 只有G5显示投影中心而非深度是主瓶颈时再细审 |

这些论文只能提供经过同行评审且有官方代码的机制来源，不能证明移植到StereoDETR必然提升。真正授权改码的依据仍是V09自身的G5结果。

## 四、G5如何避免“Oracle看起来很高但无法实现”

G5没有把整个预测框直接换成真值后就宣布有空间，而是逐组件替换：

| 诊断变体 | 固定的内容 | 替换的内容 | 对应最低成本候选 |
|---|---|---|---|
| GT轴向深度_保留预测射线 | 类别、二维框、投影中心、尺寸、朝向和分数 | 仅相机Z方向深度 | MonoDGP式轻量几何残差校正 |
| GT投影中心_保留预测深度 | 类别、二维框、深度、尺寸、朝向和分数 | 仅三维中心的图像投影 | 可见区域约束的投影中心细化 |
| GT完整三维位置 | 类别、二维框、尺寸、朝向和分数 | 深度与投影中心 | 仅用于判断二者是否存在耦合，不直接授权大模块 |
| GT三维尺寸 | 位置、朝向和分数 | 三维长高宽 | 类别条件尺寸残差头 |
| GT朝向 | 位置、尺寸和分数 | rotation_y | 周期分布式朝向细化 |
| GT全部三维属性 | 候选和分数 | 位置、尺寸、朝向 | 整体回归上限，仅用于诊断 |

匹配只使用同类别二维IoU，完全不使用被诊断的三维属性，从而避免“先用三维真值挑中最好预测，再声称三维误差很大”的循环偏差。未匹配预测保持原样，因此诊断只衡量“已有候选的组件修正能力”，不会凭空生成新候选。

## 五、预注册门槛

G5在看到结果前固定如下决策线：

1. 单组件Car Moderate Oracle至少`+0.50 AP`，并且Car、Pedestrian、Cyclist Moderate相对V09都不低于`-0.10 AP`，才允许实现对应短程配对版本。
2. 单组件为`+0.30—+0.49 AP`时，只允许零初始化、冻结主干的微型五轮验证，不运行195轮。
3. 单组件低于`+0.30 AP`则直接否决，不因为论文新、代码热门或模块易接而继续。
4. 如果只有“完整三维位置”明显、深度和投影中心单项都不明显，先分析二者耦合，不立即做大模块。
5. 任意正式候选仍需同起点、同种子控制组，随后进行第二随机种子和三组交替CUDA同步时延复测。

## 六、若深度通过，V15的严格设计边界

G5若确认轴向深度是主要瓶颈，V15才实现以下结构：

```text
立体深度 z_stereo
几何深度 z_geo = f × H3D / h2D
差异特征 Δz = normalize(z_geo - z_stereo)
查询特征 q + 深度log方差 σ + Δz
        ↓ 极小MLP（末层零初始化）
受限残差 δz
最终深度 z = z_stereo + λ × tanh(δz)
```

这里与旧V03/V04有本质区别：

- 不重新做查询级局部深度分布；
- 不直接把几何深度与立体深度平均；
- 不让随机初始化模块覆盖V09深度；
- 以V09为起点，冻结主干，只训练极小残差头；
- 推理仅增加每个查询上的小MLP，不增加卷积骨干、相关体、稠密立体网络或额外图像编码器；
- 对残差幅度做硬限制，并保留原深度log方差和V09质量排序。

这条路线借鉴MonoDGP“几何误差修正投影公式”的已验证逻辑，但把直接几何深度修正改为对强立体深度的条件残差校正，符合当前基线比单目更可靠的事实。该调整属于针对代码和任务差异的必要适配，不应声称是原样复现MonoDGP。

## 七、若深度不通过，后续如何转向

- 投影中心Oracle最高：审计`pred_boxes[...,0:2]`和采样点约束，优先做小残差中心头；不再做深度融合。
- 尺寸Oracle最高：只训练类别条件尺寸残差，并检查尺寸变化是否破坏深度几何公式。
- 朝向Oracle最高：采用周期分布或von Mises式轻量损失，推理头参数量极小。
- 所有单项都低：说明剩余主要瓶颈更可能是候选召回、匹配或多组件耦合；停止三维回归微调，重新做漏检/误检归因。

## 八、当前状态

- 已完成：V09代码路径审计、2024—2026官方论文/代码筛选、G5脚本、自检和测试入口接入。
- 尚未完成：AutoDL上对V09正式3769张验证集预测运行G5。
- 明确未做：没有创建V15正式模型、没有更改V09权重、没有开始新训练。
- 下一步唯一动作：上传本地更新并运行G5；把生成的`诊断报告.md`发回，再决定是否实现V15。
