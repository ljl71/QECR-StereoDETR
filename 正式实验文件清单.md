# 无蒸馏 QECR 正式实验文件清单

## 2026-08-14：G7通过，但V22五轮配对否决

- `lib/models/monodetr/axial_depth_iou.py`：无参数的一维轴向GIoU训练损失；
- `lib/models/monodetr/stereodetr.py`：只在最终匹配查询上计算V22辅助项，尺寸与朝向全部分离梯度；
- `versions/V22O_原始Laplace深度控制/config.yaml`：仅续训原始深度分类器的公平控制；
- `versions/V22A_轴向IoU敏感深度损失/config.yaml`：唯一变量为0.5权重的轴向GIoU；
- `scripts/运行V22轴向IoU深度损失.sh`：冒烟、配对、参数审计和自动止损汇总；
- `AutoDL_V22轴向IoU敏感深度损失实验指南.md`：服务器完整执行与结果回传命令；
- `V22轴向IoU敏感深度损失_实现与预注册.md`：证据、公式、代码位置与论文边界；
- 当前状态：V22O/V22A已完成公平配对，Car Moderate变化`-0.0144 AP`；V22正式否决，不补种子、不扫损失系数、不运行195轮。

## 2026-08-11：V12分组相关门控进入短程配对

- `tools/诊断分组相关信息_V12G0.py`：100样本正式诊断已经通过；
- `lib/models/monodetr/depth_predictor/depth_predictor_lightstereo.py`：新增416参数、基线等价初始化的s4/16组门控；
- `versions/V12O_原始相关公平控制/config.yaml`：共同训练原`cost_agg`的公平控制；
- `versions/V12A_s4十六组轻量门控/config.yaml`：共同训练范围外只新增门控；
- `scripts/运行V12轻量分组相关.sh`：冒烟、5轮配对、止损汇总及三组交替时延；
- `V12轻量分组相关门控_实现与证据记录.md`：论文边界、公式和诊断证据；
- 当前状态：仅授权短程配对；尚无V12 AP提升结论，禁止直接运行195轮。

## 零、2026-08-11 最新决策：V11否决，V12先做分组相关信息诊断

V11A 已完成与 V11O 的同起点五轮配对。V11A 的 Car 3D AP_R40 Moderate 为 `48.5817`，V11O 为 `49.6485`，下降 `1.0668 AP`；偏移参数确实更新且冻结区未变化，因此属于有效负结果。V11B、V11完整训练和动态上采样正向创新表述全部停止。

V12针对当前相关体在进入LightStereo聚合前立即对256通道求均值、可能丢失匹配子空间信息的问题。论文与官方代码审计支持分组相关方向，但正式主模型暂不修改；先用V09冻结特征和KITTI稀疏激光真值做G0诊断，只有通过预注册门槛才实现零初始化、压回原D通道的轻量门控。

| 文件 | 作用 |
|---|---|
| `tools/诊断分组相关信息_V12G0.py` | 捕获V09的s4/s8/s16左右特征，比较当前均匀平均、分组最大和逐点最优组Oracle |
| `scripts/运行V12分组相关诊断.sh` | 20样本冒烟、100样本正式诊断、统一日志与输入审计 |
| `第二创新点_V12分组相关信息保留路线论证.md` | 基线痛点、论文/官方代码映射、候选排除、正式门槛和计划结构 |
| `AutoDL第二创新点_V12分组相关诊断指南.md` | 上传检查、总测试、两级诊断和结果回传命令 |

G0的Oracle使用真实视差逐点选组，只能证明信息上限，不能写成模型结果。正式G0至少需要100个样本、5000个目标点和3000个Car点；目标框内与Car的命中率和误差改善均达到预注册门槛后，才批准V12A五轮配对，仍不批准直接跑195轮。

## 零、2026-08-11 最新决策：V10否决，进入V11动态深度上采样

V10冻结权重诊断已经证明前景Top-K深度截断会系统性破坏三类3D AP，因此不训练、不继续调K或温度。第二创新点候选转向深度分类分支的空间恢复：保留81类全分布期望，只把固定双线性上采样替换为零偏移基线保持的轻量动态采样。

| 文件 | 作用 |
|---|---|
| `lib/models/monodetr/depth_predictor/depth_predictor_lightstereo.py` | V11动态采样器及第一级/两级接入 |
| `versions/V11O_双线性深度上采样控制/config.yaml` | 相同训练范围下的双线性控制组 |
| `versions/V11A_第一级动态深度上采样/config.yaml` | 最低风险的第一级动态采样版本 |
| `versions/V11B_两级动态深度上采样/config.yaml` | 仅在V11A通过后运行的两级版本 |
| `configs/AutoDL_V11A_动态上采样冒烟.yaml` | 24样本一轮冒烟 |
| `tools/测试V11动态深度上采样.py` | 零偏移等价、偏移边界、反向传播、训练范围与学习率测试 |
| `tools/检查V11模型结构.py` | 训练前审计真实模型的可训练范围与参数量 |
| `tools/汇总V11动态深度上采样.py` | 解析正式R40并执行预注册止损判定 |
| `scripts/运行V11动态深度上采样.sh` | 冒烟、V11O/A配对、V11B门禁和自动汇总 |
| `AutoDL第二创新点_V11动态深度上采样实验指南.md` | 完整上传、测试、screen训练和回传命令 |

V11O/V11A都从同一个V09最佳checkpoint开始，只训练原深度分类分支；V09质量头保留用于推理但被冻结且关闭质量损失。先跑5周期配对，V11A相对V11O的Car Moderate至少`+0.15 AP`、Hard不低于`-0.10 AP`且两个小类别不同时下降，才允许V11B。完整训练和论文正向表述都必须等待该门槛通过。

## 零、2026-08-10 最新决策：V08止损，运行V09强基线质量组合

V08A未超过V08O公平控制，V08B造成Pedestrian随投影损失生效持续退化，V08C不再运行。V08O最佳Car 3D AP_R40 Moderate `49.5345`保留为优化续训基线，但不归因于新增模块。下一项唯一批准的训练实验为V09：冻结同一个V08O最佳checkpoint，只训练V06B点式三维质量头，并固定`beta=1.5`。

| 文件 | 作用 |
|---|---|
| `versions/V09_V08O加点式三维质量排序/config.yaml` | V08O强基线加V06B点式质量头的正式配置 |
| `scripts/运行V09强基线质量组合.sh` | 一条命令完成R0复评、R1五轮训练和结果汇总 |
| `tools/汇总V09强基线质量组合.py` | 只读取严格IoU的正式R40结果，自动生成R1−R0对照和止损判断 |
| `tools/测试Checkpoint兼容白名单.py` | 验证V09只允许V08O权重缺少`quality_head.*`，拒绝其他结构差异 |
| `AutoDL_V09强基线加三维质量排序实验指南.md` | 上传保护、完整测试、screen运行、产物位置和判定门槛 |

V09直接通过线预先固定为：相对同权重R0，Car Moderate至少`+0.10 AP`且Car Hard下降不超过`0.10 AP`。实验后不延长训练、不重新扫描beta。`49.7596`仅是历史数值机械相加形成的观察目标，不是预测或论文结果。

## 零、2026-08-09 最新决策：V07 止损，转入 V08 空间—投影协同对齐

V07“质量引导候选检索”已使用同一份 V06B 权重、同一验证集和同一质量指数完成无训练配对评估。Car 3D AP_R40 Moderate 仅由 48.5477 变为 48.5481（+0.0004），低于预先约定的 +0.10 AP 门槛，因此**否决 V07，不训练、不扫描更多 Top-K**。该结果说明 V06B 的有效收益主要来自候选进入 Top-K 后的三维质量重排序，而不是分类 Top-K 漏掉了大量高质量候选。

第二创新点候选改为训练期的空间—投影协同对齐：对 Hungarian/稳定匹配后的正查询解码三维框，加入三维角点一致性与左目 P2 投影一致性；损失按训练轮次渐进启用。该模块不增加可学习参数，不新增推理分支。双目 P3 一致性暂不加入 V08，只有 V08C 通过短程门槛后才进入后续 V09。

| 版本 | 三维角点 | 左目投影 | 用途 |
|---|---:|---:|---|
| V08O | 关 | 关 | 与 V08 完全相同训练起点、轮数和增强设置的短程公平对照 |
| V08A | 开 | 关 | 单独验证三维空间角点协同约束 |
| V08B | 关 | 开 | 单独验证二维投影闭环约束 |
| V08C | 开 | 开 | 验证空间—投影协同组合 |

V08O/A/B/C 均从同一 V00 最优权重开始，学习率固定为 2e-5，短程训练 5 轮。正式比较对象是 V08O，而不是历史 195 轮 V00。优先观察 Car 3D AP_R40 Moderate，同时记录 Easy/Hard、行人、骑行者、推理时间和训练稳定性。任一模块相对 V08O 提升不足 +0.20 AP，或波动明显且无跨难度一致改善，就不进入完整训练；V08C 达到 +0.30 AP 才考虑 195 轮/长程验证。

新增文件：

| 文件 | 作用 |
|---|---|
| `lib/models/monodetr/geometry_alignment.py` | 三维框解码、角点构造、P2 投影、角点/投影损失和渐进权重 |
| `tools/测试空间投影对齐.py` | 几何零误差、扰动响应、渐进调度及反向传播测试 |
| `configs/AutoDL_V08C_几何损失冒烟.yaml` | 24 样本一轮冒烟配置 |
| `versions/V08O_几何对齐短程对照/config.yaml` | 公平短程对照 |
| `versions/V08A_分阶段三维角点对齐/config.yaml` | 三维角点独立消融 |
| `versions/V08B_分阶段左目投影对齐/config.yaml` | 左目投影独立消融 |
| `versions/V08C_空间投影协同对齐/config.yaml` | 角点与投影组合 |
| `AutoDL第二创新点_V08空间投影对齐训练指南.md` | 上传检查、训练前测试、冒烟、短程筛选和止损命令 |


## 零、2026-08-09 当前实验决策

V05 短程结果不足以支持继续投入完整训练，ResNet50 也因增加推理时延而不采用。下一步只批准运行**不训练的 G1 三维质量排序诊断**：先验证现有 V00 候选中是否已经存在定位更好但排序靠后的 3D 框。G1 通过前不得创建或训练 V06，更不得直接跑新的 195 轮实验。

G1 文件：

| 文件 | 作用 |
|---|---|
| `tools/诊断3D质量排序_G1.py` | 一次 V00 验证集推理；统计分类分数、深度置信度、乘积分数与真实 3D IoU 的相关性，扫描 sigma 指数并计算 Oracle 排序上限 |
| `AutoDL三维质量排序G1诊断指南.md` | 上传、自检、完整运行、结果读取和自动止损门槛 |

G1 的 Oracle 使用验证集 GT，只能决定是否进入 V06，严禁写成正式模型精度。G1已经通过，后续候选为查询级轻量3D质量头、训练期3D IoU质量对齐及pair-wise排序损失；保持ResNet34，不更换骨干。

## 零点一、此前 V05 决策记录

V01、V02、V03 均未超过 V00，原查询深度路线暂停。200样本正式G0已完成：SGBM内多候选覆盖提升4.76个百分点、候选误差改善1.649 px，结论为“谨慎”。**目前仍不得训练 V04、旧 V15–V18，也不得直接跑 V05 的195轮；只批准 V05 的10轮配对筛选。**

G0证据与V05短程实验文件：

| 文件 | 作用 |
|---|---|
| `tools/诊断多候选视差监督.py` | 用稀疏 Velodyne 离线参照比较 BM 单值、16 候选、SGBM 与 LRC；不训练、不改模型 |
| `AutoDL多候选视差监督诊断指南.md` | 20 样本冒烟、200 样本正式诊断和结果回传命令 |
| `AutoDL视差监督V05短程筛选指南.md` | V05门禁、冒烟、V05A/V05D优先顺序和0.3 AP筛选门槛 |

候选最小误差只是监督集合的 oracle 覆盖能力，不能写成模型预测 EPE 或 AP 增益。V05A和V05D必须从同一个V00最优权重、以相同10轮和`2e-5`学习率进行配对，不能把短程V05D直接和历史195轮结果相减。

## 一、唯一正式入口

训练、验证和测试统一使用：

```bash
python tools/训练与评估_无蒸馏QECR.py --config <版本配置>
```

`tools/训练与评估_QECR.py` 是被正式入口调用的内部实现，不单独运行。旧 QDR 工程和教师缓存脚本不属于本轮实验。

## 二、正式代码

| 文件 | 作用 |
|---|---|
| `tools/训练与评估_无蒸馏QECR.py` | 无蒸馏训练与评估总入口 |
| `lib/models/monodetr/query_depth.py` | 查询局部分布、跨层细化与基线安全深度融合 |
| `tools/QECR运行时引导.py` | 注册经过测试的极线模块 |
| `lib/models/monodetr/query_epipolar_runtime.py` | 查询级局部极线搜索、双向一致性、置信度和深度分布融合 |
| `lib/models/qecr_model.py` | 将 QECR 接入 StereoDETR，输出逐层校正结果并计算无教师监督损失 |
| `lib/datasets/kitti/qecr_kitti_dataset.py` | 从 P2/P3 计算网络尺度 `fb`，处理裁剪、缩放和左右翻转 |
| `lib/helpers/qecr_dataloader_helper.py` | 构造带双目标定信息的数据加载器 |
| `lib/helpers/qecr_model_helper.py` | 构造 QECR 模型和损失 |
| `lib/helpers/qecr_trainer_helper.py` | 训练和验证适配 |
| `lib/models/monodetr/ops/setup_qecr.py` | 按当前 GPU 架构编译 CUDA 算子 |
| `lib/datasets/kitti/kitti_dataset.py` | 真正启用所配置的BM/SGBM；训练期整理4×4内16候选并计算LRC软置信度 |
| `lib/models/monodetr/depth_predictor/disparityloss.py` | 将16候选合并为96通道概率质量并计算置信度加权交叉熵 |
| `tools/测试多候选视差监督.py` | 检查匹配器切换、候选无损整理、概率归一化、排列不变性和反向传播 |
| `lib/models/monodetr/quality_ranking.py` | V06的66,561参数质量头、相机坐标3D IoU目标、点式和成对排序损失 |
| `tools/测试3D质量排序.py` | 检查V06质量头反向传播、目标解码和最终分数融合 |
| `AutoDL轻量三维质量排序V06实验指南.md` | V06C优先的冒烟、五轮筛选、止损和V06B消融命令 |

基线安全融合只增加3个可学习标量，对应3层解码器；查询级极线校正本身没有可学习参数；V18 的不确定性门控仅增加 8,481 个可学习参数。它不会建立全图代价体，也不增加第二套图像骨干网络。

V05A–V05D不增加任何可学习参数。SGBM、16候选整理和LRC只发生在训练数据加载及辅助视差损失中，模型前向和推理输出保持V00不变。

V06增加66,561个查询级质量头参数；训练时冻结V00，推理时不计算3D IoU。50个推理query约增加3.29M MAC，真实时延仍必须在同一4090上同步测量。

## 三、正式配置与消融含义

| 版本 | 查询分布 | 安全融合 | 跨层细化 | 单向极线 | 往返一致性 | 五点采样 | 极线感知门控 |
|---|---:|---:|---:|---:|---:|---:|---:|
| V00 | 关 | 关 | 关 | 关 | 关 | 关 | 关 |
| V01 | 开 | 关 | 关 | 关 | 关 | 关 | 关 |
| V02 | 开 | 关 | 开 | 关 | 关 | 关 | 关 |
| V03 | 开 | 开 | 关 | 关 | 关 | 关 | 关 |
| V04 | 开 | 开 | 开 | 关 | 关 | 关 | 关 |
| 旧 V15 | 开 | 关 | 开 | 开 | 关 | 关 | 关 |
| 旧 V16 | 开 | 关 | 开 | 开 | 开 | 关 | 关 |
| 旧 V17 | 开 | 关 | 开 | 开 | 开 | 开 | 关 |
| 旧 V18 | 开 | 关 | 开 | 开 | 开 | 开 | 开 |

V05独立于上表的查询模块，所有查询和推理期极线开关都关闭：

| 版本 | 匹配器 | 视差标签 | LRC | 当前权限 |
|---|---|---|---|---|
| V05A | BM | 单值 | 无 | 10轮续训对照，第一阶段先跑 |
| V05B | SGBM | 单值 | 无 | V05D通过后补跑 |
| V05C | SGBM | 16候选概率质量 | 无 | V05D通过后补跑 |
| V05D | SGBM | 16候选概率质量 | 软权重 | 10轮完整筛选，第一阶段第二个跑 |

配置目录仅使用：

```text
versions/V00_官方基线
versions/V01_查询级局部分布
versions/V02_跨层分布细化
versions/V03_基线安全查询深度
versions/V04_基线安全跨层细化
versions/V15_单向局部极线校正
versions/V16_双向极线一致性
versions/V17_双向一致性加多点采样
versions/V18_无蒸馏完整模型
versions/V05A_短程续训对照
versions/V05B_SGBM单值修正
versions/V05C_SGBM多候选监督
versions/V05D_多候选加LRC软权重
versions/V06B_点式三维质量排序
versions/V06C_点式加成对排序
```

V06当前顺序：先跑机制完整的V06C五轮；通过`+0.30 AP`门槛后，再补V06B作为点式损失消融。

## 四、代码执行顺序

```text
左右图像
  → StereoDETR 共享骨干与解码器
  → 查询级局部深度概率分布
  → V03/V04 将查询深度作为受限残差写回原始深度
  → V04 可选跨解码层分布细化
  → 基于预测深度的局部极线采样
  → 可选右到左往返一致性检验
  → 熵与循环误差联合置信度
  → 局部立体分布和查询深度分布融合
  → V18 不确定性感知几何门控
  → 3D 检测输出
```

V03/V04 的融合公式为 `d = d_base + α(d_query - d_base)`。每层一个 `α`，初始为0.05、上限为0.50，因此查询分支不能再完全替换基线深度。旧V15—V18尚未迁移到安全融合路径，当前不得作为V03/V04后续直接运行。

## 五、当前 AutoDL 公平实验环境

所有版本复用 V00 基线已经验证的 `stereodetr-open` 环境，并固定使用 Batch Size 12。工程已经合入 PyTorch 2.0 的 Attention 接口兼容和 Matcher CPU/GPU 布尔索引兼容修复。这些修复只保证新环境可运行，不属于创新模块。

## 六、正式检查

上传 AutoDL、编译 CUDA 算子后先运行：

```bash
bash scripts/运行QECR测试.sh
```

本地检查覆盖配置矩阵、基线安全融合初值与权重上界、概率归一化、前向/反向传播、越界回退、P2/P3 标定、三层包装集成和门控组合。完整 KITTI 训练结果仍必须在 AutoDL 上实测，不能在实验完成前预设精度增益。
