# V12轻量分组相关门控：实现与证据记录

## 结论

StereoDETR的三个相关体在进入轻量二维聚合器前，均对256个特征通道直接求均值。V12-G0证明该平均操作确实丢失了大量可区分的匹配信息，但固定选择最大组会降低匹配质量。因此正式V12不是简单max或扩大代价体，而是只在G0支持的s4尺度保留16组相关响应，并用极轻量空间门控压回原来的24个视差通道。

截至当前，V12只获得进入5轮同起点配对的授权，尚未形成AP提升结论。

## 2026-08-12 AutoDL冒烟结果

V12A在RTX 4090、PyTorch 2.0.1+cu118环境完成24个训练样本、12个验证样本的一轮冒烟：

- 退出码：0；训练与checkpoint复评均完成；
- 新增门控参数：416；实际学习率组：`2e-5`与`2e-4`；
- V09 checkpoint只缺少预注册的3个门控张量，兼容白名单通过；
- 新增张量：3；允许范围内发生变化的张量：204；冻结区变化：0；
- `group_logits_abs_max=0.0021392815`，证明门控收到梯度并发生更新；
- 无Traceback、OOM、NaN或Inf。

日志中的Car AP_R40 Moderate `77.9115`来自仅12个样本的冒烟子集，没有统计意义，禁止作为精度提升、消融结果或论文数据。`loss_depth=-2.06`来自原有异方差深度损失中的可学习`log_variance`项；在全部数值有限且训练完成的条件下允许为负，不属于V12错误。

冒烟结论：**通过进入V12O/V12A五轮同起点配对**。

## G0正式诊断证据

| 项目 | 数值 |
|---|---:|
| 固定样本 | 100 |
| 成功/失败 | 100 / 0 |
| 稀疏参照点 | 60983 |
| 最佳尺度/组数 | s4 / 16 |
| 目标点 | 24022 |
| Car点 | 20372 |
| 目标原均值MAE | 2.3753 bin |
| 目标Oracle MAE | 0.6238 bin |
| 目标原均值±1命中 | 57.07% |
| 目标Oracle ±1命中 | 87.10% |
| Car原均值MAE | 2.3909 bin |
| Car Oracle MAE | 0.6454 bin |
| Car原均值±1命中 | 55.74% |
| Car Oracle ±1命中 | 86.39% |

分组最大聚合在目标区域的MAE为3.5310 bin、±1命中率44.96%，均差于原均值。因此G0支持“学习如何选组”，不支持“直接取最大组”。

## 模块逻辑

设左右特征为`L,R∈R^(C×H×W)`，`C=256`，分为`G=16`组。每个视差候选先计算组内相关：

```text
c_g(d,x,y) = mean_c(L_g(x,y) · R_g(x-d,y))
```

门控从左特征各组的局部RMS能量生成空间权重：

```text
s_g = sqrt(mean_c(L_g²))
w = softmax(Conv1x1(GELU(DWConv3x3(s))))
C(d,x,y) = Σ_g w_g(x,y)c_g(d,x,y)
```

`Conv1x1`的权重和偏置全部置零，因此初始`w_g=1/G`，有：

```text
Σ_g (1/G)c_g = mean_c(L·R)
```

即初始前向恢复原始StereoDETR相关体。门控仅416个参数；每次只构建一个视差切片，不保存五维组相关体。

## 接入位置

- 文件：`lib/models/monodetr/depth_predictor/depth_predictor_lightstereo.py`；
- 原位置：`correlation_volume()`生成s4相关体；
- 新模块：`BaselinePreservingGroupwiseCorrelation`；
- 只替换s4，s8和s16保持原始逻辑；
- 训练增强中的flip/switch路径使用与左特征样本对齐的门控切片；
- 推理输出形状仍为`[B,24,H/4,W/4]`，后续`cost_agg`无需改接口。

## 公平实验设计

| 版本 | 相关体 | 训练参数 |
|---|---|---|
| V12O | 原全通道均值 | 原`depth_predictor.cost_agg.*` |
| V12A | s4、16组轻量门控 | 与V12O相同的`cost_agg.*`，外加416个门控参数 |

两组均从同一个V09最佳checkpoint开始，随机种子、数据、batch size、学习率和5轮训练长度一致。V09质量头保留用于最终排序，但冻结且不计算质量损失。新门控使用10倍参数学习率，其余共同参数学习率均为`2e-5`。

## 止损条件

V12A相对V12O必须满足：

1. Car 3D AP_R40 Moderate至少`+0.15`；
2. Car Easy不低于`-0.20`；
3. Car Hard不低于`-0.10`；
4. Pedestrian、Cyclist Moderate均不低于`-0.50`；
5. 精度通过后，三组交替顺序同步时延的Median增幅不超过3%。

若只提高`0.05–0.15`，结论为“谨慎”，必须补第二随机种子；若未达到则停止，不运行195轮。

## 可引用边界

- G0结果只能写成设计动机或离线诊断，不能作为检测AP；
- Oracle使用激光真值逐点选组，禁止作为网络性能；
- 只有V12A正式KITTI验证集结果超过V12O并完成时延复核后，才能把V12写成论文创新点；
- 在此之前应表述为“候选模块”，不能表述为“有效改进”。

## 2026-08-12 五轮同起点配对结果

V12O 与 V12A 均从同一个 V09 最佳 checkpoint 出发，采用同一随机种子、相同数据、相同五轮训练长度，并共同更新 `depth_predictor.cost_agg.*`；V12A 只额外更新 416 个分组相关门控参数。KITTI Chen 验证集正式 3D AP_R40 如下：

| 类别 | V12O E/M/H | V12A E/M/H | V12A−V12O E/M/H |
|---|---:|---:|---:|
| Car | 68.6912 / 49.5777 / 41.7485 | 68.8633 / 49.7656 / 41.8422 | +0.1721 / **+0.1879** / +0.0937 |
| Pedestrian | 32.9977 / 24.8181 / 20.5385 | 32.2533 / 24.3199 / 19.2632 | -0.7444 / -0.4982 / -1.2753 |
| Cyclist | 42.8924 / 23.0436 / 21.9299 | 42.1688 / 22.8926 / 20.9096 | -0.7236 / -0.1510 / -1.0203 |

按照实验前固定的门槛，V12A 已达到“精度通过”：Car Moderate 提升超过 `+0.15 AP`，Car Easy/Hard 以及 Pedestrian、Cyclist Moderate 均未触发止损线。Car 三个难度同时提高，说明收益不是单个 Moderate 排序点的孤立波动。

该结论仍属于候选通过，而不是最终论文结论。Pedestrian/Cyclist Hard 分别下降 `1.2753/1.0203 AP`；Hard 未被原预注册小类别保护项覆盖，不能在论文分析中省略。下一步必须完成同卡交替顺序同步前向时延，以及第二随机种子的 V12O/V12A 配对。只有第二种子仍保持 Car Moderate 正增益且两种子平均收益成立，才授权将 V12写为第二创新点并考虑最终组合实验。

## 2026-08-12 第一次正式时延测试

在同一张 RTX 4090 上，以 batch=1、20 次预热、100 次 CUDA 同步计时，按照 O→A、A→O、O→A 的顺序完成三组测试：

| 版本 | 三组 Median (ms) | Median平均 (ms) | 三组中位数 (ms) |
|---|---:|---:|---:|
| V12O | 25.1428 / 24.4808 / 24.5207 | 24.7148 | 24.5207 |
| V12A | 25.6772 / 26.4617 / 29.9770 | 27.3720 | 26.4617 |

V12A 的三组 Median 平均增幅为 `+10.75%`，三组中位数增幅为 `+7.92%`，均超过预注册的 `+3%` 上限，因此原始实现时延未通过。参数量只从 `18,459,785` 增至 `18,460,201`（`+416`），说明主要问题不是模型规模，而是实现对每个视差切片重复执行了分组重排、组内归约和跨组加权。

为区分“方法固有代价”和“实现冗余”，在不改变 checkpoint、参数、门控权重或输出定义的前提下进行了代数等价优化：先把每组权重除以组内通道数并一次性展开到左特征通道，随后每个视差切片只执行与基线相同的单次通道归约。新单元测试同时比较非均匀门控下的旧公式与优化公式。该优化无需重训；必须保留第一次测速，上传后以独立输出目录重新完成三组交替复测。若复测仍超过 3%，V12 按低时延目标停止，不以 416 参数掩盖实际运行代价。

## 2026-08-12 等价优化后正式时延复测

使用原 V12O/V12A checkpoint、相同 RTX 4090 和相同三组交替顺序重新计时：

| 版本 | 三组 Median (ms) | Median平均 (ms) | 三组中位数 (ms) |
|---|---:|---:|---:|
| V12O | 24.4680 / 25.1824 / 24.9586 | 24.8697 | 24.9586 |
| V12A | 24.6847 / 24.8702 / 25.5246 | 25.0265 | 24.8702 |

V12A 三组 Median 平均增幅降至 `+0.63%`，三组中位数差异为 `-0.35%`，正式通过预注册的 `+3%` 时延门槛。该复测没有重新训练、没有修改 checkpoint，也没有改变 416 个门控参数，证明初测的 `+10.75%` 主要来自逐视差重复张量归约，而非方法固有计算量。论文可报告优化后实现的同步前向时延，但必须同时保存初版结果作为工程实现审计，不能把它删除后假装从未出现。

当前状态更新为：第一随机种子精度通过、低时延通过，授权运行种子445公平复核。第二种子在训练前收紧保护规则：除复现 Car Moderate 配对收益外，还要求两种子平均的 Pedestrian/Cyclist Moderate 与 Hard 不出现系统性退化，并单独判断两个 V12A 的 Car Moderate 平均是否真正超过 V09 的 `49.8210`。

## 2026-08-12 第二随机种子后的分层结论

种子445中，V12A相对同种子V12O的Car Moderate提高`+1.0244 AP`；与种子444的`+0.1879 AP`同号，两种子平均配对增益为`+0.6061 AP`。因此，如果研究问题明确限定为KITTI车辆三维检测，V12具备“跨两个种子可复现的Car主指标增益 + 416参数 + 0.63%同步前向代价”的积极证据。

但是，预注册的三类别保护没有通过：两种子平均Pedestrian/Cyclist Moderate分别为`-0.3716/-0.6346 AP`，Hard分别为`-0.8553/-0.6351 AP`；两个V12A的Car Moderate绝对值平均`49.6294`，也没有超过V09的`49.8210`。因此不得声称V12带来多类别普适提升，也不得把当前V12A作为刷新V09的最终组合。

这里必须区分两类证据：V09是“强续训起点 + 第一创新点”的组合结果，不是官方基线；模块贡献应优先由同种子V12A−V12O衡量，而最终模型是否刷新历史最高值是另一项更严格要求。为判断两者之间的差距是否来自共同续训`cost_agg`，下一步只做无训练门控移植诊断：把两个V12A的三个门控张量分别移植到逐位保持的V09，其余参数全部恢复V09。若仍能提升，才开发仅训练416参数的V12B；否则停止V12。原三类别“未通过”结论保留，不因Car结果积极而事后改写。

## 2026-08-12 门控独立性移植诊断与最终决策

两个V12A checkpoint中仅`context.weight`、`group_logits.weight`和`group_logits.bias`三个门控张量分别移植回原始V09；所有V09共享张量在保存前逐位验证不变，整个诊断没有训练。结果如下：

| 门控来源 | Car E/M/H相对V09 | Pedestrian E/M/H相对V09 | Cyclist E/M/H相对V09 |
|---|---:|---:|---:|
| seed444 | -0.1806 / -0.1056 / -0.0460 | -0.1325 / -0.0881 / -0.1968 | +0.4102 / +1.1843 / +0.2711 |
| seed445 | -0.1765 / -0.1959 / -0.0417 | -0.1325 / -0.0881 / -0.1968 | +0.4102 / +1.1843 / +0.2711 |

Car Moderate平均下降`0.1507 AP`，两个门控均越过预注册的`-0.05 AP`止损线，门控单独训练路线因此没有获得授权。Cyclist三档稳定提高说明分组响应包含类别相关信号，但也证明当前由左特征能量产生的单一共享门控具有明显类别偏置；不能只选择Cyclist收益而忽略Car/Pedestrian下降，把它包装成普适提升。

最终决策：**V12作为V09上的最终第二创新正式停止。** 不再训练仅门控V12B，不扫描组数、门控学习率、卷积核或额外种子，不运行195轮。V12保留为完整负消融：G0证明信息上限，配对训练证明与`cost_agg`存在共同适配，移植实验进一步证明门控本身不能独立增强V09。论文若引用，只能用于方法边界或类别偏置分析，不能列入最终有效模块表。
