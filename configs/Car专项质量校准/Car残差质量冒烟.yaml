base_config: T3_trainval_Car残差质量.yaml

model_name: qlqc_car_residual_smoke

dataset:
  train_split: train
  train_txt: /root/autodl-tmp/datasets/KITTI/object/training/ImageSets/train_car_quality_smoke.txt
  batch_size: 16

trainer:
  max_epoch: 1
  max_train_batches: 2
  resume_if_exists: false
  save_final_checkpoint: true
  save_path: outputs/Car专项质量校准/冒烟测试/
