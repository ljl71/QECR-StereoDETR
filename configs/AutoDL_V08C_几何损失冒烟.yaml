base_config: ../versions/V08C_空间投影协同对齐/config.yaml

model_name: qecr_v08c_geometry_smoke

dataset:
  train_txt: /root/autodl-tmp/datasets/KITTI/object/training/ImageSets/train_v08_smoke.txt
  eval_txt: /root/autodl-tmp/datasets/KITTI/object/training/ImageSets/train_v08_smoke.txt
  batch_size: 2
  workers: 2

model:
  geometry_alignment:
    start_epoch: 0
    ramp_epochs: 1

trainer:
  max_epoch: 1
  save_frequency: 1
  save_all: false
  save_path: outputs/第二创新点_V08空间投影对齐/冒烟测试/
