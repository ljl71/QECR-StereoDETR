# V23本地差异与测试审计

## 1. V23在原项目内的文件差异

在合并前对冻结备份和V23候选代码的共同文件逐一计算SHA256。审计排除了`.git`、`.idea`、`outputs`、日志与Python缓存。

- 原有文件缺失数：0
- 原有文件修改数：5
- V23新增文件数：21（包含本审计文档）

仅以下五个继承文件发生修改：

1. `configs/QECR默认配置.yaml`
2. `lib/models/monodetr/depth_predictor/depth_predictor_lightstereo.py`
3. `lib/models/monodetr/stereodetr.py`
4. `scripts/运行QECR测试.sh`
5. `tools/检查QECR消融配置.py`

其余继承文件与原项目逐文件一致。新增文件只包括V23配置、G8诊断、单元测试、参数审计、结果汇总、总控脚本和文档。

## 2. Windows本地检查

已通过：

- 所有V23 Python文件的`py_compile`语法检查。
- 六套V23短程配置和一套V23Q配置的递归解析。
- QECR完整配置矩阵检查，其中六套V23配对配置全部通过。
- V23配对结果解析与预注册止损自检。
- V23双随机种子复核自检。
- V23三组交替时延和参数上限自检。
- V23Q最终权重选择规则自检。

未在Windows本地执行：

- PyTorch张量前向与反向测试。
- 完整StereoDETR模型构建。
- Bash语法检查和AutoDL CUDA测试。

原因是当前Windows Python 3.12环境未安装PyTorch，也没有Linux Bash。上述检查已全部纳入AutoDL的`preflight`，训练脚本在任一检查失败时会停止。

## 3. AutoDL必须通过的检查

`bash scripts/运行V23分组视差预聚合.sh preflight`将验证：

- 四组均匀平均数值恢复原始全通道相关。
- 普通方向和翻转方向的视差构建一致。
- RDSA和GPSD零初始化时输出与原始相关逐位相同。
- 零初始化末端能够获得有限且非零梯度。
- RDSA参数量为666，GPSD参数量为25584。
- V23O/A/B只有预注册参数可训练。
- 候选checkpoint只允许缺少`depth_predictor.cost_preaggregation_s4.*`。
- 所有训练、汇总和时延脚本通过Bash语法检查。

只有AutoDL `preflight`退出码为0，才允许执行G8与后续短程实验。
