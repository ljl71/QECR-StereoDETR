# V23分组保真空间视差预聚合项目说明

## 1. 改进目标

StereoDETR在每个视差位置先对全部通道相关性求平均，再交给LightStereo式二维代价聚合器。该操作计算开销低，但不同通道组所对应的匹配响应在进入聚合网络前已经被压缩为一个标量。V12曾证明分组信息具有离线Oracle上限，但其门控在代价聚合前直接把分组压回单通道，双随机种子实验中小类别退化，且门控独立移植没有稳定收益。

V23不再学习分组权重后立即求和，而是保留分组轴与视差轴，经过轻量空间混合后再产生残差修正。同时，原始StereoDETR相关体作为恒等主路完整保留。

## 2. 三组公平短程实验

### 2.1 V23O原始相关公平控制

V23O从固定V09 checkpoint出发，只续训原有`depth_predictor.cost_agg`五轮。它衡量共同续训本身带来的波动。

### 2.2 V23A残差式视差空间微聚合

V23A把标量相关体视为`[B,1,D,H,W]`，使用三层3D卷积构造局部视差空间残差：

\[
C_A=C_0+R_{3D}(C_0).
\]

最后一个归一化层采用零初始化，所以训练前`C_A=C_0`。该分支只有666个参数，用于判断一般性的局部视差聚合是否已经足够。

### 2.3 V23B分组保真空间视差预聚合

对第`g`组和视差`d`，分组相关定义为：

\[
C_g(d,p)=\frac{1}{|\mathcal{G}_g|}\sum_{c\in\mathcal{G}_g}
F_c^L(p)F_c^R(p-d).
\]

V23B在`s4`保留四组、二十四个视差假设，将`[G,D]`折叠为九十六个二维通道。随后使用`1×1`扩展、`3×3`深度卷积和`1×1`投影构成轻量空间—视差混合器：

\[
C_B=C_0+P_{GPSD}(\operatorname{Fold}(C_g)).
\]

投影末端归一化参数置零，保证新建模型第一次前向严格恢复V09标量相关主路。V23B新增25584个参数，约占1846万参数基线的0.139%。

## 3. G8只读信息诊断

正式训练前，G8使用100个固定抽样的KITTI验证样本、冻结V09特征和Velodyne稀疏投影视差，仅判断四组相关中是否存在被全通道平均抹去的信息。G8不训练模型，激光点不进入检测器，Oracle结果不得写成检测精度。

只有同时满足以下条件才允许短程配对：

1. 成功样本不少于100个。
2. 目标框内有效稀疏点不少于10000个，Car点不少于8000个。
3. 目标区域Oracle的±1 bin命中率至少提升5个百分点，MAE至少相对下降10%。
4. Car Oracle的±1 bin命中率至少提升3个百分点，MAE至少相对下降10%。

## 4. 短程与复核判定

第一随机种子中，V23B相对V23O的Car 3D AP_R40 Moderate至少提升0.20，同时相对V23A至少提升0.10，才可把收益归因于分组保真。Car Easy和Hard下降不得超过0.10，Pedestrian与Cyclist的Moderate和Hard下降不得超过0.20。

第二随机种子要求V23B相对V23O的Car Moderate至少提升0.10，两种子平均至少提升0.20，两种子平均相对V23A至少提升0.10，并继续满足多类别保护线。

同步CUDA batch=1模型前向的三组Median平均增幅不得超过3%，参数增幅不得超过0.5%。所有门槛通过后才运行三轮V23Q查询级质量重标定。若V23Q未提升Car Moderate或破坏多类别保护，则最终保留V23B，不采用V23Q。

## 5. 论文边界

- G8和任何使用真值选择分支的Oracle只用于开发决策。
- 论文消融只能使用V23O、V23A、V23B、V23Q的正式KITTI验证集结果。
- 在双种子与时延门槛通过前，GPSD只能称为待验证候选，不能写成已证实贡献。
- 官网测试结果必须使用最终固定训练协议重新训练和一次性提交，不能用KITTI test调参。
